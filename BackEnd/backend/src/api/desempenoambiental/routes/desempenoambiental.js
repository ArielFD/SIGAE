'use strict';

/**
 * desempenoambiental router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::desempenoambiental.desempenoambiental');
