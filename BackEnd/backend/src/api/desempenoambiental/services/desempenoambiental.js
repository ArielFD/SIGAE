'use strict';

/**
 * desempenoambiental service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::desempenoambiental.desempenoambiental');
